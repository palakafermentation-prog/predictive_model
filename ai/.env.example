# AI Worker Configuration
MODEL_MODE="mock"
# MODEL_MODE="live"

# Path to trained model artifacts (required when MODEL_MODE=live)
# Must be within the ai/ workspace unless MODEL_DIR_ALLOW_EXTERNAL is set
# MODEL_DIR="ai/ml/models/trained"

# Set to 1 to allow MODEL_DIR paths outside the ai/ workspace (e.g., in production deployments)
# SECURITY: MODEL_DIR points to joblib/pickle files — only trust paths you fully control
# MODEL_DIR_ALLOW_EXTERNAL="0"
